<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

    <!-- bean definitions here -->
    <bean id="carSpecification" class="car.example.constructor.injection.Specification">
        <property name="make" value="Toyota"/>
        <property name="model" value="Corolla"/>
    </bean>

    <bean id="myCar" class="car.example.constructor.injection.Car">
        <!--
        Carクラスのコンストラクタに
        「carSpecification」というBeanを引数として渡す。

        ref="carSpecification" は、
        value="..." のように文字列を渡すのではなく、
        Springコンテナに登録されている
        carSpecification Beanへの参照を渡すことを意味する。
    -->
        <constructor-arg ref="carSpecification"/>
    </bean>

</beans>