<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
		https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context
		https://www.springframework.org/schema/context/spring-context.xsd">

    <!--
    com.example.componentscan パッケージ以下を
    SpringがComponent Scanする。

    Component Scanによって、例えば

        @Component
        public class Employee {
        }

    のように@Componentが付いているクラスを自動的に検出し、
    Spring ContainerのBeanとして登録する。

    そのため、EmployeeについてXMLで

        <bean id="employee"
              class="com.example.componentscan.Employee"/>

    と明示的に定義する必要がない。

    また、@Componentだけでなく、以下のような
    Springのステレオタイプアノテーションも対象になる。

        @Component
        @Service
        @Repository
        @Controller
        @RestController

    base-package="com.example.componentscan"
        ↓
    このパッケージと、そのサブパッケージをスキャンする。
-->
    <context:component-scan base-package="com.example.componentscan"/>

</beans>